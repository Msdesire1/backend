/**
 * Leftover scratch file — safe to delete.
 *
 * This was a temporary import-timing probe. It exports nothing and does nothing;
 * it is still here only because this environment is not permitted to delete files
 * in your folders. Remove it whenever you like:
 *
 *   del probe.tmp.mjs
 */
export {};
